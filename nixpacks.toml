[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm install --production=false']

[phases.build]
cmds = ['npm run build', 'cd backend && npm install --production']

[start]
cmd = 'NODE_ENV=production node backend/server-simple.js'
